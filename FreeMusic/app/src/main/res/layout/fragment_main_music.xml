<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="?attr/mainColor"
    android:orientation="vertical">

    <!--    <android.support.design.widget.TabLayout
            android:id="@+id/fragment_main_music_tab"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="?attr/toolbar_background"
            android:paddingBottom="5dp"
            app:tabIndicatorColor="?attr/tab_select"
            app:tabSelectedTextColor="?attr/tab_select"
            app:tabTextColor="?attr/tab_unselect" />-->
    <include
        layout="@layout/tablayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <android.support.v4.view.ViewPager
        android:id="@+id/fragment_main_music_pager"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

</LinearLayout>